name: school-lunch

services:
  api:
    build:
      context: ./src/api
      dockerfile: Dockerfile
    image: school-lunch/api:latest
    container_name: school-lunch-api
    restart: unless-stopped
    environment:
      # Required: provide via shell env or a .env file next to this compose file.
      NEIS_API_KEY: ${NEIS_API_KEY:?NEIS_API_KEY is required}
      NEIS_BASE_URL: ${NEIS_BASE_URL:-https://open.neis.go.kr/hub}
      # Requests reach the API only via the web/nginx reverse proxy,
      # so same-origin is the only allowed CORS origin in this setup.
      CORS_ORIGINS: '["http://localhost:8080"]'
      PORT: "8000"
    expose:
      - "8000"
    networks:
      - school-lunch
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request,sys;\
sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/api/health',timeout=2).status==200 else 1)"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=64m
    cap_drop:
      - ALL

  mcp:
    build:
      context: ./src
      dockerfile: mcp/Dockerfile
    image: school-lunch/mcp:latest
    container_name: school-lunch-mcp
    restart: unless-stopped
    environment:
      NEIS_API_KEY: ${NEIS_API_KEY:?NEIS_API_KEY is required}
      NEIS_BASE_URL: ${NEIS_BASE_URL:-https://open.neis.go.kr/hub}
      PORT: "8000"
    ports:
      - "127.0.0.1:${MCP_PORT:-8001}:8000"
    networks:
      - school-lunch
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request,sys;\
sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/health',timeout=2).status==200 else 1)"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=64m
    cap_drop:
      - ALL

  web:
    build:
      context: ./src/web
      dockerfile: Dockerfile
    image: school-lunch/web:latest
    container_name: school-lunch-web
    restart: unless-stopped
    environment:
      # nginx envsubst rewrites this into /etc/nginx/conf.d/default.conf at startup.
      API_UPSTREAM: http://api:8000
    ports:
      - "${WEB_PORT:-8080}:8080"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - school-lunch
    healthcheck:
      test:
        - CMD
        - wget
        - -qO-
        - http://127.0.0.1:8080/
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
      - /var/cache/nginx:size=32m,uid=101,gid=101
      - /var/run:size=4m,uid=101,gid=101
      - /etc/nginx/conf.d:size=4m,uid=101,gid=101
    cap_drop:
      - ALL

networks:
  school-lunch:
    driver: bridge
