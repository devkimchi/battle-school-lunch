# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
# Azure deployment topology is defined in apphost.mts and applied with `aspire deploy`.
# azd does not import TypeScript AppHosts, so this file only retains project identity.
name: school-lunch
metadata:
  template: school-lunch-aspire@1.0.0
